# --- secrets: never commit environment files ---
.env
**/.env
.env.prod
# Nginx Basic Auth password file (created on the server)
deploy/htpasswd

# --- python ---
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# --- runtime data (regenerated by the automations) ---
09 Automation/market/market.db
09 Automation/scripts/history.json

# --- obsidian workspace state (per-machine UI noise) ---
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# --- os ---
Thumbs.db
desktop.ini

# Bot fleet paper ledger (runtime data)
backend/data/

# Vault folders copied into the backend build context at deploy time
backend/vault/

